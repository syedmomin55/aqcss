import 'package:flutter/material.dart';

Gradient primaryblue = LinearGradient(
  colors: [Color(0xff2C8EF5), Color(0xff1D71F7)],
  begin: FractionalOffset(0.0, 0.0),
  end: FractionalOffset(1.0, 0.0),
  stops: [0.0, 1.0],
  tileMode: TileMode.clamp,
);

Gradient primarygreen = LinearGradient(
  colors: [Color(0xff4CB884), Color(0xff239C5C)],
  begin: FractionalOffset(0.0, 0.0),
  end: FractionalOffset(1.0, 0.0),
  stops: [0.0, 1.0],
  tileMode: TileMode.clamp,
);
Gradient primarydarkblue = LinearGradient(
  colors: [Color(0xff1C2898), Color(0xff7E76D6)],
  begin: FractionalOffset(0.0, 0.0),
  end: FractionalOffset(1.0, 0.0),
  stops: [0.0, 1.0],
  tileMode: TileMode.clamp,
);
